{
  "name": "shared-lambda-layer",
  "version": "1.0.0",
  "description": "Shared layer containing Gmail API dependency",
  "scripts": {
    "package": "npm ci && cd .. && zip -r shared-layer.zip nodejs/"
  },
  "dependencies": {
    "@googleapis/gmail": "^5.0.0"
  }
} 